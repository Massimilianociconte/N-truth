# Legacy and challenger profiles

Profiles in this directory are never selected implicitly. Pass `--profile`
explicitly and satisfy the same Reality Gate authorization as the provisional
primary profile before any substantive training.
