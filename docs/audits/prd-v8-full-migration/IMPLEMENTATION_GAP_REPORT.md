# PRD v8.0 implementation gap report — Phase 1

**Base:** `origin/main@fe089eff42c16e3fa55606be340c85df57c5442b`  
**Conclusion before implementation:** `PARTIAL`, with critical incompatible scientific paths.

## What is worth preserving

- Local-first packaging, safe-ingest controls, content-addressed provenance, privacy and
  licence governance.
- Append-only conflict/confirmation mechanics, once global authority precedence is removed.
- Family-aware split connected components and synthetic train-only policy.
- The isolated MVT-A candidate-stage schema, the neutral `AnalysisHandoff` component,
  Quick Design IDs/export foundations, burden records and hostile-input tests.
- Explicit project HOLD pending real anchors. Engineering work must not change that science
  and data readiness state.

## Critical incompatible paths

1. `graph/units.py` uses biological-source independence as a proxy for inferability and n.
2. Active parser/training contracts predict and score determinability directly.
3. The current Rulebook is effectively the source of scientific theory.
4. Determinability is block/global rather than claim/query-specific.
5. Bare `None`, empty strings/lists and string `unknown` encode scientific absence.
6. The report/UI can make completeness/determinability look like a green design verdict.
7. Statistical strategy suggestions are emitted despite the required `HANDOFF_ONLY` status.
8. TEST/EXTERNAL_CHALLENGE records can be training eligible.
9. Reality Gate v7 can open without v8 semantic, reference, E2E and contamination evidence.

## Missing contract layers

- Core Semantic Kernel, KnowledgeState and profile predicate closure.
- Canonical count/query/event-timing contracts and full event graph.
- Independent Derivation Theory, Theory Reference Set roles and conformance harness.
- DerivedClaim/Set, report resolution, adequacy, scenario/profile coverage and sensitivities.
- Planned/executed reconciliation and v8 report/UI/CLI paths.
- Contamination attestation, ReportBundle E2E evaluation, blind residual audit and
  cluster-aware confidence intervals.
- Machine-readable current-to-target architecture, release blockers, example conformance and
  clean-checkout documentation truth.

## Scope boundary

This migration may implement schemas, adapters, deterministic mechanics, fixtures, tests,
gates and documentation. It cannot manufacture Real Anchor cases, Derivation Gold,
reference-stability results, contamination evidence, Strategy Reference Set validation or an
independent scientific review. Those remain explicit blockers, not missing code disguised as
defaults. No training, model download, new dataset acquisition, external dataset mutation or
FLASH128 mutation is in scope.

## Target status

The maximum honest outcome for this repository-only task is
`IMPLEMENTED_WITH_EXPLICIT_BLOCKERS`: engineering-complete v8 contracts with scientific/data
release gates remaining closed. `FULLY_IMPLEMENTED` would require the external evidence and
reviews listed in `SCIENTIFIC_REVIEW_REGISTER.md`.
